# AGENTS.md

TCMT 主仓库：硬件监控系统（client 采集 / server 中转存储 / viewer 展示），三个子模块各自独立成仓库。

## Agent skills

### Issue tracker

Issues live in GitHub Issues（`dongge0210/TCMT`，用 `gh` CLI 读写）。See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context 布局：根目录 `CONTEXT.md` + `docs/adr/`。See `docs/agents/domain.md`.
